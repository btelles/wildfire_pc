= form_for @bicycle do |f|
  -if @bicycle.errors.any?
    #error_explanation
      %h2= "#{pluralize(@bicycle.errors.count, "error")} prohibited this bicycle from being saved:"
      %ul
        - @bicycle.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
